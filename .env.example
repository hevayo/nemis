# =============================================================================
# NEMIS Monorepo Environment Configuration
# =============================================================================
# Copy to .env:  cp .env.example .env

# --- Web (Vite) ---
WEB_PORT=5173
VITE_API_URL=http://localhost:8080

# --- API (Laravel) ---
API_PORT=8080
APP_ENV=local
APP_KEY=

# --- Database (MySQL) ---
DB_PORT=3307
DB_DATABASE=nemis
DB_USERNAME=nemis
DB_PASSWORD=nemis1234
DB_ROOT_PASSWORD=root

# --- WSO2 Ports ---
# Single-instance mode (nemis-app container)
WSO2_SSH_PORT=2222

# Separate-instance mode
APIM_SSH_PORT=2222
IS_SSH_PORT=2223

# Console and gateway (shared across modes)
APIM_CONSOLE_PORT=9443
IS_CONSOLE_PORT=9444
APIM_GATEWAY_PORT=8243
